# ==========================
# 0. WIPE ALL DEFAULT BINDINGS
# ==========================
# This removes every default Prefix keybinding
unbind -a

# ==========================
# 1. General Settings & Prefix
# ==========================
# Set prefix to Ctrl+a
set -g prefix C-b
bind C-b send-prefix
# Enable mouse mode (lets you scroll history with the mouse wheel)
set -g mouse on
# WheelUpPane: If not in copy-mode, enter copy-mode and scroll up
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"
# WheelDownPane: Standard scroll down
bind -n WheelDownPane select-pane -t= \; send-keys -M
# Fix escape delay (crucial if you use Vim/Neovim)
set -sg escape-time 10
# Increase scrollback buffer size
set -g history-limit 50000

# ==========================
# 2. Window Numbering
# ==========================
# Start windows at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
# Automatically renumber windows when one is closed
set -g renumber-windows on

# ==========================
# 3. THE ESSENTIAL PREFIX COMMANDS
# ==========================
# Prefix + w : Show an interactive list of all windows
bind w choose-tree -w
# Prefix + n : Create a new window
bind n new-window
# Prefix + x : Kill the current window (with y/n confirmation)
bind x confirm-before -p "kill-window #W? (y/n)" kill-window
# Prefix + d : Detach from the tmux session (to leave it running in background)
bind d detach-client
# Prefix + : : Open the tmux command prompt
bind : command-prompt
# Prefix + [ : Enter copy/scroll mode using the keyboard
bind [ copy-mode

# ==========================
# 4. Window Navigation (Prefix Required)
# ==========================
# Prefix + h or Left Arrow to go to the previous window
bind h previous-window
bind Left previous-window
# Prefix + l or Right Arrow to go to the next window
bind l next-window
bind Right next-window
# Prefix + 1-9 to jump to a specific window directly
bind 1 select-window -t 1
bind 2 select-window -t 2
bind 3 select-window -t 3
bind 4 select-window -t 4
bind 5 select-window -t 5
bind 6 select-window -t 6
bind 7 select-window -t 7
bind 8 select-window -t 8
bind 9 select-window -t 9


bind-key T new-window
bind-key W kill-window
bind-key H previous-window
bind-key L next-window

bind-key -- - split-window -v -c "#{pane_current_path}"
bind-key '\' split-window -h -c "#{pane_current_path}"

# ==========================
# 5. Minimalist Status Bar
# ==========================
set -g status-position bottom
set -g status-right ""
set -g status-left ""
# 'default' makes the status bar background transparent (matches your terminal)
set -g status-bg default
# Inactive window text is the normal color (e.g., blue)
set -g window-status-format " #[fg=white]#I:#W "
# Active window text is the bright color and bold
set -g window-status-current-format " #[bold,fg=brightblue]#I:#W "

# ==========================
# 6. Easy Reloading
# ==========================
# Prefix + r : Reload config on the fly
bind r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded"
